--- a/drivers/net/phy/Kconfig
+++ b/drivers/net/phy/Kconfig
@@ -486,3 +486,14 @@ config MICREL_KS8995MA
 
 config MT753X_GSW
 	tristate "Driver for the MediaTek MT753x switch"
+
+config AIROHA_EN8811H_SDK_PHY
+	tristate "Airoha EN8811H 2.5G Gigabit PHY"
+	help
+	  Currently supports the Airoha EN8811H PHY using MTK SDK.
+
+config AIROHA_EN8811H_PHY_DEBUGFS
+	bool "EN8811H debugfs support"
+	depends on AIROHA_EN8811H_SDK_PHY
+	help
+	  Enable creation of debugfs files for the EN8811H drivers.
--- a/drivers/net/phy/Makefile
+++ b/drivers/net/phy/Makefile
@@ -110,3 +110,4 @@ obj-$(CONFIG_TERANETICS_PHY)	+= teraneti
 obj-$(CONFIG_VITESSE_PHY)	+= vitesse.o
 obj-$(CONFIG_XILINX_GMII2RGMII) += xilinx_gmii2rgmii.o
 obj-$(CONFIG_MT753X_GSW)	+= mtk/mt753x/
+obj-$(CONFIG_AIROHA_EN8811H_SDK_PHY)	+= en8811h/
